## API Report File for "cloneman"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Console as Console_2 } from 'node:console';

// @public
export interface BuildContext {
    buildTemplate(this: void, name: string, config?: TemplateConfig | NormalizedTemplateConfig): Promise<BuildTemplateResult>;
    readonly logger: Console_2;
    readonly targetDir: string;
    readonly templateDir: string;
}

// @public (undocumented)
export interface BuildTemplateResult {
    readonly files: string[];
    renovateIgnoreDependencies(): Promise<void>;
    updateJson(this: void, filePath: string, content: unknown): Promise<void>;
    writeFile(filePath: string, content: string): Promise<void>;
}

// @public
export interface NormalizedTemplateConfig {
    [normalizedConfig]: true;
    ignoredDependencies: string[];
    ignoredFiles: string[];
    managedFiles: string[];
    uninstallDependencies: string[];
}

// @public
export function pack(options: {
    cwd: string;
    targetDir: string;
    env?: Record<string, string>;
}): Promise<void>;

// @public
export interface PackageJson {
    // (undocumented)
    cloneman?: unknown;
    // (undocumented)
    dependencies?: Partial<Record<string, string>>;
    // (undocumented)
    description?: string;
    // (undocumented)
    devDependencies?: Partial<Record<string, string>>;
    // Warning: (ae-forgotten-export) The symbol "ExportsField" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    exports?: ExportsField;
    // (undocumented)
    name: string;
    // (undocumented)
    scripts?: Record<string, string>;
    // (undocumented)
    type?: "module" | "commonjs";
    // (undocumented)
    version: string;
}

// @public
export function prepare(templateDir: string, targetDir: string): Promise<{
    output: string;
}>;

// @public
export function publish(options: {
    cwd: string;
    env?: Record<string, string>;
    npmRcPath?: string;
}): Promise<void>;

// @public
export function readConfigFile(filePath: string): Promise<NormalizedTemplateConfig>;

// @public
export function readJsonFile<T = unknown>(filePath: string): Promise<T>;

// @public
export function readPackageJson(dir: string): Promise<PackageJson>;

// @public
export interface TemplateConfig {
    ignoredDependencies?: string[];
    ignoredFiles?: string[];
    managedFiles?: string[];
    uninstallDependencies?: string[];
}

// (No @packageDocumentation comment for this package)

```
