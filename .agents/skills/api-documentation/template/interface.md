# ${name} interface

${description}

```ts
interface $ {
    name;
}
{
    /* include each property in alphabetical order */
    /* include each method in alphabetical order */
}
```

Include each property in alphabetical order using [property.md](./property.md).

Include each method in alphabetical order using [method.md](./method.md).
