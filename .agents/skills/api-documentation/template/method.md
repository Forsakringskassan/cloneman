## ${name} method

${description}

**Syntax**

```js
${prototype};
```

**Parameters**

: `${parameter}: ${type}`${optional suffix}
${parameter description}
Default `${default}`.
<!-- Omit the preceding line when no default is known. -->

: `${parameter}.${property}: ${type}`${optional suffix}
${property description}
Default `${default}`.
<!-- Omit the preceding line when no default is known. -->

**Return value**

Returns ${return value}.

A promise resolved with ${return value}.

**Example:**

${example}

${outcome}
